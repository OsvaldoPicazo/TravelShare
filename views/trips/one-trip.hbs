<h1> Trip details</h1>

<div class="trip-card">

</div>
<h2>Name: {{trip.name}}</h2>
<p>Description: {{trip.description}}</p>
<img class="trip-img" src="{{trip.imageUrl}}" alt="{{trip.name}}" />

<h4>Owner: {{trip.owner.username}}</h4>

<br />
<br />
<h2>Want to leave a comment?</h2>
<form action='/trips/{{trip._id}}' method='POST'>

  <textarea type='text' name='comment'></textarea>

  <button type="submit">Send comment</button>

</form>



{{#if trip.reviews}}
<h3>List of reviews:</h3>

{{#each trip.reviews}}

  <h4>Author: {{this.user.username}}</h4>
  <h5>Author: {{this.user.email}}</h5>
  
  <p>Comment: </p>
  <p>{{this.comment}}</p>

{{/each}}
{{/if}}